"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createUuid = exports.stringify = exports.trunc = exports.trim = exports.isString = void 0;
const uuid_1 = require("uuid");
const function_1 = require("fp-ts/lib/function");
const Option_1 = require("fp-ts/lib/Option");
exports.isString = (value) => typeof value === 'string' || value instanceof String;
exports.trim = (strings, ...values) => {
    const notNilValues = values.map(function_1.flow(Option_1.fromNullable, Option_1.getOrElse(() => '')));
    const interpolation = strings.reduce((prev, current) => prev + current + (notNilValues.length ? notNilValues.shift() : ''), '');
    return interpolation.trim();
};
exports.trunc = (n) => (input) => (input.length > n)
    ? input.substr(0, n) + '…'
    : input;
exports.stringify = (value) => typeof value === 'function'
    ? (value.displayName || value.name)
    : JSON.stringify(value);
exports.createUuid = () => uuid_1.v4();
